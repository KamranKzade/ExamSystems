@model NewExam

@{
    Layout = "_Layout";
}

<div class="col-12">
    <div class="bg-secondary rounded h-100 p-4">
        <form asp-action="AddExam" asp-controller="Home" method="post">
            <div class="form-group">
                <label for="courseCode" class="form-label">Course Code</label>
                <select asp-for="CourseCode" class="form-control" asp-items="@(ViewBag.CourseCodes as SelectList)" id="courseCode"></select>
                <span asp-validation-for="CourseCode" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label for="studentNumber" class="form-label">Student Number</label>
                <select asp-for="StudentNumber" class="form-control" asp-items="@(ViewBag.StudentNumbers as SelectList)" id="studentNumber"></select>
                <span asp-validation-for="StudentNumber" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label for="examDate" class="form-label">Exam Date</label>
                <input type="date" asp-for="ExamDate" class="form-control mb-2 bg-dark" id="examDate" />
                <span asp-validation-for="ExamDate" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-primary">Accept</button>
        </form>
    </div>
</div>
